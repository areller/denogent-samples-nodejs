# automatically generated by denogent

name: build
'on':
  push:
    branches:
      - master
jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis
        ports:
          - '6379:6379'
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: setup deno
        uses: denolib/setup-deno@v2
        with:
          deno-version: v1.4.1
      - name: Install NodeJS latest
        uses: actions/setup-node@v1
      - name: run build
        run: >-
          deno run -A -q --unstable build/build.ts --serial --skip-services
          --runtime gh-actions run
        env:
          REDIS_HOST: localhost
          REDIS_PORT: '6379'
          REDIS_PORTS: '6379'
